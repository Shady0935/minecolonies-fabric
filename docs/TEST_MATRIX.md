# Test matrix

Commands are run from the target workspace using Java 17 and PowerShell.

| Area | Status | Evidence |
|---|---|---|
| Workspace/reference checkout | `AUTOMATED_PASS` | Reference repositories and commit map are present under `references/`; implementation is isolated under `project/`. |
| Fabric dependency projects | `AUTOMATED_PASS` | Structurize and Domum Ornamentum Fabric 1.20.1 builds passed; their dev jars load with the target. |
| Java compilation | `AUTOMATED_PASS` | `project/minecolonies/gradlew.bat compileJava --no-daemon` passed after the Fabric lifecycle and gameplay callback bridges. |
| Fabric 1.20.1 package build | `AUTOMATED_PASS` | `project/minecolonies/gradlew.bat build --no-daemon` passed after the native advancement, icon and quest providers; the final JAR contains 1,453 JSON resources, 152 advancements, 9 quests, 2,609 generated citizen/raider icon textures, 19 `data/c` conventional-tag files, no packaged datagen cache and no Forge `mods.toml`/access-transformer metadata. |
| Datagen | `AUTOMATED_PASS` | `runDatagen` completed with 37 providers and 3,576 generated resources: 967 JSON files, 2,609 icon PNGs and 19 generated `data/c` conventional-tag files. Evidence is in `logs/minecolonies-native-datagen-providers.log`. |
| Generated resource parity | `AUTOMATED_PASS` | Native Fabric datagen now emits the advancements, quests, translations and entity icons previously supplied by the audit snapshot. A semantic audit found 705 equal shared JSON files and 243 documented Fabric adaptations; the client reload in `logs/minecolonies-client-generated-resources.log` reaches OpenAL and all texture atlases. The snapshot remains only for comparison. |
| Generated resource JSON | `AUTOMATED_PASS` | Recursive parsing of source plus generated resources succeeded for 1,462 JSON files with zero parse failures; generated custom ingredient arrays use Fabric's `fabric:any` serializer. |
| Dedicated server startup | `AUTOMATED_PASS` | `logs/minecolonies-native-datagen-providers.log` reaches `Done` with the native-generated resource set, loads 1,480 total advancements and has no client-classloading or missing-tag crash. |
| Dedicated save/stop/restart | `AUTOMATED_PASS` | The same `run/world` restored MineColonies colony data, the native-provider rerun found `EntityCitizen` Atticus A. Barnes with colony/citizen NBT, accepted `save-all`, stopped with all dimensions saved, and completed with `BUILD SUCCESSFUL`; evidence is in `logs/minecolonies-native-datagen-providers.log` plus the focused restart logs. |
| Client bootstrap | `AUTOMATED_PASS` | `logs/minecolonies-native-datagen-providers.log` records the final `runClient --no-daemon` reaching resource reload, OpenAL and all texture atlases with native generated resources, the spear renderer, extended menu registrations and gameplay mixins loaded; the run was then stopped after bootstrap. |
| Spear built-in renderer registration | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | The Fabric built-in renderer is registered and constructed lazily after `EntityModelSet` becomes available; client bootstrap passes in `logs/minecolonies-client-spear-renderer-fixed.log`, while inventory/GUI appearance still needs an in-game visual check. |
| Client/server login | `AUTOMATED_PASS` | `project/minecolonies/run/logs/latest.log` records `PortTest logged in`, `New Server UUID`, `PortTest joined the game`, and clean chunk saves. |
| Networking decoder path | `AUTOMATED_PASS` | `logs/minecolonies-gametest-network-codec.log` round-trips `OpenDecoBuildWindowMessage`, verifies server registration of representative S2C messages, reassembles an out-of-order `ServerUUIDMessage`, and confirms cache invalidation. |
| Client-to-server split envelope | `AUTOMATED_PASS` | `logs/minecolonies-gametest-c2s-envelope.log` serializes `TownHallRenameMessage`, routes it through the real Fabric server receiver boundary and split-packet executor, verifies owner permission and colony-name mutation, then confirms cache cleanup. |
| Client-to-server research action | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-research.log` serializes `TryResearchMessage`, routes it through the real Fabric server receiver boundary with a registered `SERVERBOUND` player connection, verifies owner permission, non-creative diamond-cost consumption and `IN_PROGRESS` state, then confirms cache cleanup; the University GUI and broader research actions remain manual. |
| Client-to-server colony foundation | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-create-colony.log` serializes `CreateColonyMessage`, routes it through the real Fabric server receiver boundary with a registered `SERVERBOUND` player connection, creates the owner colony, registers the Town Hall and preserves the requested structure pack/blueprint; the in-game Town Hall GUI remains manual. |
| Client-to-server Builder work-order request/selection | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-builder-selection.log` serializes `BuildRequestMessage` in `REPAIR` mode and `BuilderSelectWorkOrderMessage`, routes both through the real split envelope/server executor with a registered `SERVERBOUND` owner, verifies the live `JobBuilder` precondition, creates/claims the Town Hall `WorkOrderBuilding`, assigns it to the citizen job and confirms cache cleanup; Builder GUI placement, materials and AI navigation remain manual. |
| Client-to-server direct Town Hall placement | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-direct-place.log` serializes `DirectPlaceMessage`, routes it through the real split envelope/server executor, places the Town Hall for the owner, consumes the supplied item, preserves the default style, resolves `fundamentals/townhall1.blueprint` and confirms cache cleanup; the client placement GUI and visual workflow remain manual. |
| Client-to-server decoration build request | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-decoration.log` serializes `DecorationBuildRequestMessage`, routes it through the real split envelope/server executor, asynchronously resolves `fundamentals/townhall1.blueprint`, registers a `WorkOrderDecoration` for the owner with `BUILD`, Colonial style, clockwise rotation, mirror and target level 1, and confirms cache cleanup; the decoration GUI and actual Builder placement remain manual. |
| Client-to-server hut/building rename | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-hut-rename-fixed.log` serializes `HutRenameMessage`, routes it through the real split envelope/server executor, resolves the Town Hall building for its owner, mutates the custom name and confirms cache cleanup; the BlockUI rename screen remains manual. |
| Client-to-server colony style settings | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-colony-style.log` serializes `ColonyNameStyleMessage`, `ColonyStructureStyleMessage` and `ColonyTextureStyleMessage`, routes all three through real split envelopes/server execution, mutates the live colony style fields and confirms all cache entries are cleaned; the colony banner/style GUI remains manual. |
| Client-to-server colony management settings | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-colony-management.log` serializes `ToggleHousingMessage`, `ToggleJobMessage` and `ColonyFlagChangeMessage`, routes them through real split envelopes/server execution, mutates manual housing/hiring allocation and banner patterns, and confirms all cache entries are cleaned; the Town Hall management GUI remains manual. |
| Client-to-server Builder delivery priority | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-delivery-priority-isolated.log` serializes `ChangeDeliveryPriorityMessage` twice, routes increase/decrease envelopes through real split execution against a Builder with a worker module, verifies the live pickup priority and confirms both cache entries are cleaned; the Builder logistics GUI remains manual. |
| Client-to-server Farmer field assignment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-farmer-fields.log` serializes `AssignmentModeMessage` and `AssignFieldMessage`, routes manual-mode, assign and free envelopes through real split execution against a Farmer and `FarmField`, verifies module/ownership transitions and confirms all cache entries are cleaned; the Farmer GUI remains manual. |
| Client-to-server building settings | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-farmer-settings.log` serializes `TriggerSettingMessage` with a `BoolSetting`, routes it through real split execution against a Farmer settings module, disables fertilizer requests and confirms cache cleanup; the generic building-settings GUI remains manual. |
| Client-to-server building reactivation | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-reactivate.log` serializes `ReactivateBuildingMessage`, routes the position-only request through real split execution against a deactivated Builder hut, re-registers `BuildingBuilder`, removes the `deactivated` tag and confirms cache cleanup; the in-game reactivation screen remains manual. |
| Client-to-server Miner level selection | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-miner-level.log` serializes `MinerSetLevelMessage`, routes the selected level through real split execution against a Colonial Miner, persists `currentLevel` in `MinerLevelManagementModule` and confirms cache cleanup; the miner level GUI and shaft-construction cycle remain manual. |
| Client-to-server Miner level repair | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-miner-repair.log` serializes `MinerRepairLevelMessage`, routes a valid level repair through real split execution against a Miner with ladder/cobble tags and a populated level module, creates the associated shaft `WorkOrderMiner` and confirms cache cleanup; actual Builder placement and mining AI remain manual. |
| Client-to-server Guard Tower patrol mine | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-guard-mine.log` serializes `GuardSetMinePosMessage`, assigns a real Miner to a Guard Tower, clears the patrol target through the no-position form and confirms both cache entries are cleaned; the guard patrol GUI and navigation/combat cycle remain manual. |
| Client-to-server building dirty marking | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-dirty-building.log` serializes `MarkBuildingDirtyMessage`, routes the empty-payload request through real split execution against a Town Hall after clearing its initial dirty state, verifies the building becomes dirty and confirms cache cleanup. |
| Client-to-server colony move-in policy | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-citizen-controls-retry.log` serializes `ToggleMoveInMessage`, disables and re-enables a real colony's move-in policy through separate split envelopes and confirms both cache entries are cleaned; the Town Hall GUI remains manual. |
| Client-to-server citizen pause/resume | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-citizen-controls-retry.log` serializes `PauseCitizenMessage`, pauses and resumes a live citizen through separate split envelopes and confirms both cache entries are cleaned; the worker-management GUI remains manual. |
| Client-to-server Farmer hire/fire | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-hire-fire.log` serializes `HireFireMessage`, hires and fires the same live citizen through separate split envelopes against a Farmer worker module, verifies the `JobFarmer` transition and confirms both cache entries are cleaned; the worker-management GUI remains manual. |
| Client-to-server Farmer hiring mode | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-hiring-mode.log` serializes `BuildingHiringModeMessage`, switches the Farmer worker module from `DEFAULT` to `AUTO` and back through separate split envelopes, verifies both transitions and confirms cache cleanup; the worker-management GUI remains manual. |
| Client-to-server creative citizen skill adjustment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-adjust-skill.log` serializes `AdjustSkillCitizenMessage`, routes a Strength increment through the real split envelope for a creative owner, verifies the live citizen skill mutation and confirms cache cleanup; the citizen-management GUI remains manual. |
| Client-to-server Courier hiring mode | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-hiring-modes.log` serializes `CourierHiringModeMessage`, switches a real Warehouse courier-assignment module from `DEFAULT` to `AUTO` and back through separate split envelopes, verifies both transitions and confirms cache cleanup; the Warehouse GUI remains manual. |
| Client-to-server Quarry hiring mode | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-hiring-modes.log` serializes `QuarryHiringModeMessage`, switches a real Simple Quarry module from `DEFAULT` to `AUTO` and back through separate split envelopes, verifies both transitions and confirms cache cleanup; the Quarry GUI and construction/mining cycle remain manual. |
| Client-to-server work-order priority/removal | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-work-order-change.log` serializes `WorkOrderChangeMessage`, updates the priority of a registered Town Hall `WorkOrderBuilding` and then removes it through separate split envelopes, verifies both `WorkManager` mutations and confirms cache cleanup; Town Hall work-order GUI interaction remains manual. |
| Client-to-server residence assignment/unassignment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-assign-unassign.log` serializes `AssignUnassignMessage`, registers a Colonial residence and live citizen, assigns and unassigns the citizen through separate split envelopes, verifies `LivingBuildingModule` membership and the citizen home relationship, and confirms both cache entries are cleaned; the residence GUI and automatic housing cycle remain manual. |
| Client-to-server free-interaction permissions | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-free-interaction.log` serializes `ChangeFreeToInteractBlockMessage`, adds and removes a chest block and an absolute position through separate split envelopes, verifies both `Colony` permission collections and confirms all cache entries are cleaned; the Town Hall permissions GUI and protection interaction remain manual. |
| Client-to-server Builder inventory transfer | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-transfer-items.log` serializes `TransferItemsRequestMessage`, seeds eight cobblestones in a non-creative owner inventory, transfers them into a registered Builder inventory, verifies the source is consumed and confirms split-cache cleanup; inventory GUI, request resolution and Builder logistics AI remain manual. |
| Client-to-server minimum-stock module updates | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-minimum-stock.log` serializes `AddMinimumStockToBuildingModuleMessage` and `RemoveMinimumStockFromBuildingModuleMessage`, adds three oak planks to a registered Builder `MinimumStockModule`, removes them by runtime module id, verifies both transitions and confirms split-cache cleanup; minimum-stock GUI, request resolution and logistics AI remain manual. |
| Client-to-server Guard Tower entity filters | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-entity-filter.log` serializes `AssignFilterableEntityMessage`, resolves the Guard Tower entity-list module by runtime ID, adds and removes `minecraft:zombie`, verifies both transitions and confirms split-cache cleanup; the filter GUI and patrol/combat cycle remain manual. |
| Client-to-server Smeltery item filters | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-item-filter.log` serializes `AssignFilterableItemMessage` and `ResetFilterableItemMessage`, adds/removes/re-adds an iron-ore filter in the Smeltery ore-list module, resets it to defaults, verifies every transition and confirms split-cache cleanup; the item-filter GUI and smelting/logistics cycle remain manual. |
| Client-to-server GiveTool inventory binding | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-give-tool.log` serializes `GiveToolMessage`, creates/selects a compass for the Town Hall owner, verifies the target position and colony ID in NBT and confirms split-cache cleanup; the client tool GUI and actual scepter/tool interaction remain manual. |
| Client-to-server citizen inventory transfer | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-citizen-inventory-retry.log` serializes `TransferItemsToCitizenRequestMessage`, moves seven cobblestones into a live citizen inventory, verifies the owner inventory is consumed and confirms split-cache cleanup; the citizen inventory GUI and worker logistics cycle remain manual. |
| Client-to-server single-citizen recall | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-recall-citizen.log` serializes `RecallSingleCitizenMessage`, moves a live citizen away, recalls it to the Town Hall safe spawn point, verifies `lastPosition` and confirms split-cache cleanup; navigation and Town Hall citizen GUI interaction remain manual. |
| Client-to-server assigned-citizen worker/hut recall | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-recall-assigned-stable.log` serializes `RecallCitizenMessage` and `RecallCitizenHutMessage`, routes both variants against a real Builder with an assigned live citizen, verifies the Builder safe spawn point, preserves assignment and confirms split-cache cleanup; citizen navigation and Builder GUI interaction remain manual. |
| Client-to-server crafting recipe/menu routes | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-crafting-final.log` serializes `AddRemoveRecipeMessage`, `ChangeRecipePriorityMessage`, `ToggleRecipeMessage` and `OpenCraftingGUIMessage`, adds/reorders/toggles recipes in a real Stone Smeltery, opens the furnace crafting menu and confirms split-cache cleanup; recipe GUI rendering and the worker crafting cycle remain manual. |
| Client-to-server colony control routes | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-colony-control.log` serializes `ToggleHelpMessage`, `TeamColonyColorChangeMessage` and `HireSpiesMessage`, toggles progress notices, changes the team color, consumes five gold ingots and enables spies, then confirms split-cache cleanup; the Town Hall control GUI and raid/spy cycle remain manual. |
| Client-to-server citizen-inventory menu | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-open-inventory-retry.log` serializes `OpenInventoryMessage`, registers a level-one Town Hall and live citizen, opens `ContainerCitizenInventory` through the Fabric menu bridge and confirms split-cache cleanup; client inventory rendering and interaction remain manual. |
| Client-to-server deconstructed-building style | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-building-style.log` serializes `BuildingSetStyleMessage`, routes it through the real split envelope/server executor, resolves a deconstructed Town Hall, applies the loaded `Urban Savanna` pack and confirms cache cleanup; the building-style GUI remains manual. |
| Client-to-server FarmField configuration | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-c2s-farm-fields.log` serializes `FarmFieldRegistrationMessage`, `FarmFieldUpdateSeedMessage` and `FarmFieldPlotResizeMessage`, routes them through real split envelopes/server execution, creates a scarecrow-backed field, applies a carrot seed and east radius 3, and confirms all cache entries are cleaned; the Farmer GUI and crop AI remain manual. |
| Fishing-hook spawn state | `AUTOMATED_PASS` | `logs/minecolonies-gametest-fishing-hook.log` records the 13-test batch synchronizing a citizen angler entity ID through `SynchedEntityData`; the client-side hook fixture preserves the ID after the spawn-state round-trip. |
| Extended screen-handler opening buffers | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-extended-screen-handlers.log` verifies all seven MineColonies menu types use Fabric's extended type and preserves a non-trivial opening payload; actual client GUI rendering and interaction remain manual. |
| Fabric lifecycle bridge | `AUTOMATED_PASS` | Fabric callbacks dispatch retained MineColonies server/client ticks, world/chunk/entity lifecycle, commands, login/logout and datapack events; compilation and `logs/minecolonies-runserver-lifecycle.log` are clean. |
| Forge-shaped event dispatch order | `AUTOMATED_PASS` | `logs/minecolonies-gametest-event-priority.log` verifies `HIGHEST` → `HIGH` → `NORMAL` → `LOW` → `LOWEST` ordering, inherited listener discovery and clean unregistering on the retained Fabric event bus. |
| Fabric gameplay callback bridge | `AUTOMATED_PASS` | `FabricGameplayHooks` compiles and is loaded during `logs/minecolonies-gameplay-hooks-startup.log`; it connects real Fabric callbacks for interaction, attack, pre-break/pre-place, dimension change, damage, death and mob conversion. |
| Cancellable Forge gameplay events | `AUTOMATED_PASS` | `logs/minecolonies-gametest-explosion-damage-retry.log` reports 12/12 required tests; narrow hooks verify cancellation of item pickup, item toss and farmland trampling, while the bucket-use path posts `FillBucketEvent` before vanilla behavior. |
| Hostile spawn and explosion protection bridges | `AUTOMATED_PASS` | `logs/minecolonies-gametest-research-tree.log` records the latest 13/13 run: `NaturalSpawner` dispatch/cancellation, the `DAMAGE_PLAYERS`/`DAMAGE_ENTITIES` living-damage policy, `ExplosionEvent.Start` cancellation under `DAMAGE_NOTHING`, and `ExplosionEvent.Detonate` filtering of a claimed-colony block plus a non-living item entity before vanilla damage/destruction; the Town Hall fixture resolves a real Colonial blueprint without Structurize directory-read or rotation errors. |
| Forge bow-hook compatibility | `AUTOMATED_PASS` | `logs/minecolonies-gametest-arrow-nock.log` verifies neutral `ArrowNock` behavior lets the Pharao Scepter start using, a listener-supplied action is returned, cancellation maps to `FAIL`, and retained `ArrowLooseEvent` cancellation returns `-1` to stop the shot. |
| Projectile-impact event bridge | `AUTOMATED_PASS` | `logs/minecolonies-gametest-projectile-impact.log` verifies `ForgeEventFactory.onProjectileImpact` dispatch, cancellation, original hit-result identity and listener removal. |
| Fishing-loot event bridge | `AUTOMATED_PASS` | `logs/minecolonies-gametest-fishing-event.log` verifies `NewBobberEntity`'s `ItemFishedEvent` preserves generated drops, neutral rod damage, listener-modified damage, cancellation and listener removal. |
| Server JSON listeners | `AUTOMATED_PASS` | Startup and `/reload` rebuild 130 worker recipes, 201 research recipes, quests and 1,455 item-NBT compatibility rules without custom ingredient or reload errors. |
| Research tree API | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-research-tree.log` verifies the four default branches, branch-qualified research IDs, branch metadata and the citizen-cap effect through `IGlobalResearchTree`; selecting and completing research through the University GUI remains manual. |
| Research manager selection/progression | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-research-cycle.log` selects `minecolonies:civilian/ambition` with a non-creative player, consumes its diamond cost, advances it to completion and verifies `effects/blockhutmysticalsite`; the latest 33-test core batch also registers a real University, assigns `JobResearch` and advances progress through `BuildingUniversity.onColonyTick`; researcher AI walking/mana behavior and GUI interaction remain manual. |
| Supply loot modifier | `AUTOMATED_PASS` | `logs/minecolonies-gametest-supply-loot.log` samples the configured vanilla dungeon and shipwreck tables, observes both MineColonies supply items, and verifies `Placement:"instant"` NBT. |
| Colony protection callbacks | `AUTOMATED_PASS` | `logs/minecolonies-gametest-arrow-hooks.log` drives Fabric's `UseBlockCallback` through the Town Hall permission handler: an outsider is denied and the colony owner is accepted in an isolated fixture. |
| Mob conversion callback and tavern visitor replacement | `AUTOMATED_PASS` | `logs/minecolonies-gametest-entity-persistence.log` dispatches Fabric's `MOB_CONVERSION` callback into the retained `LivingConversionEvent.Pre` bridge, leaves an unrelated conversion intact, and verifies that a populated tavern creates one `VisitorCitizen`, assigns it to the tavern and discards the vanilla candidate. |
| Command/entity smoke | `AUTOMATED_PASS` | `/minecolonies help`, `/minecolonies colony list`, `/summon minecolonies:citizen` and `/data get entity` executed on the dedicated server. The isolated summoned citizen is intentionally discarded because it has no colony assignment. |
| Datapack reload | `AUTOMATED_PASS` | `/reload` re-ran the six MineColonies server-data listeners with the native-generated advancements, quests and conventional tags present; the citizen probe, `save-all` and clean stop passed. Evidence is in `logs/minecolonies-native-datagen-providers.log`. |
| Secure/authenticated multiplayer | `MANUAL_VALIDATION_PENDING` | Development credentials returned HTTP 401, so the smoke test used temporary offline mode and restored secure defaults afterward. |
| Registry/resource bootstrap | `AUTOMATED_PASS` | Common/client entrypoints load registries, recipes, sounds, particles, tags, the packaged generated MineColonies parity resources and models; no Forge model-loader or MineColonies missing-sound errors remain. |
| Town Hall GUI/colony creation | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | Direct server-side creation plus the real C2S `CreateColonyMessage` route, ownership lookup, Town Hall registration, protection callback, populated-tavern visitor conversion and NBT round-trip pass in `logs/minecolonies-gametest-c2s-create-colony.log` and the entity-persistence log; C2S Builder request/selection, direct Town Hall placement, decoration work-order creation and hut rename are covered in `logs/minecolonies-gametest-c2s-builder-selection.log`, `logs/minecolonies-gametest-c2s-direct-place.log`, `logs/minecolonies-gametest-c2s-decoration.log` and `logs/minecolonies-gametest-c2s-hut-rename-fixed.log`; GUI/client interaction remains pending. |
| Citizen entity registration/serialization | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-citizen.log` spawns the registered `EntityCitizen`, links it to `CitizenData`, serializes identity/position and reloads the citizen from colony NBT; rendering and work AI remain pending. |
| Complete custom entity registry contract | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-entity-persistence.log` enumerates all 25 current `minecolonies:*` entity types, constructs each one, verifies its registered type identity and exercises NBT reload; the upstream non-persistent projectiles discard by design, while AI, combat, rendering and legacy migration remain manual/pending. |
| Citizen rendering/work cycle | `MANUAL_VALIDATION_PENDING` | Requires client rendering and an in-game work-cycle pass. |
| Builder work-order registration and assignment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-builder-assignment.log` verifies the Colonial Town Hall blueprint path, full-footprint claim precondition, persistent `WorkManager` ID and lookup, live `JobBuilder` assignment and `BuildingBuilder.searchWorkOrder()` claim persistence; Builder AI navigation, material requests and actual placement remain manual. |
| Residence/housing registration and assignment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-residence.log` verifies a Colonial house blueprint, `LivingBuildingModule` assignment and the citizen home relationship; the C2S assign/unassign route is covered separately in `logs/minecolonies-gametest-c2s-assign-unassign.log`; automatic housing capture, sleep/navigation and GUI remain manual. |
| Warehouse/courier registration and resolver wiring | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-warehouse.log` verifies Colonial warehouse and courier blueprints, live `JobDeliveryman` assignment to both modules, warehouse access and the Warehouse/Delivery/Pickup resolver registrations; request resolution, inventory transfer, courier AI and logistics GUI remain manual. |
| Miner work-order registration and assignment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-miner.log` verifies a Colonial miner blueprint, live `JobMiner` assignment, persistent `WorkOrderMiner` registration and `BuildingMiner.searchWorkOrder()` claim selection; shaft construction, ore/mining AI, resource delivery and the miner GUI remain manual. |
| Farmer field registration and assignment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-farmer.log` verifies a Colonial farmer blueprint, scarecrow-backed seeded `FarmField`, live `JobFarmer` assignment and `FarmerFieldsModule` ownership persistence; crop growth, harvesting AI, placement, resource delivery and the farming GUI remain manual. |
| Guard Tower registration and knight assignment | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-guard-assignment.log` verifies the Colonial `military/guardtower1.blueprint`, default defense position/patrol task, knight `GuardBuildingModule`, live `JobKnight` assignment and guard-roster persistence; guard navigation/combat AI, equipment, raids and combat outcomes remain manual. |
| RaidManager eligibility and event registration | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | `logs/minecolonies-gametest-raider-manager.log` raises a real colony above the minimum raid level, creates a forced barbarian `IColonyRaidEvent` with a spawn point and verifies the disabled-events gate; raider spawning, navigation, guard response and combat outcomes remain manual. |
| Builder/blueprint placement | `MANUAL_VALIDATION_PENDING` | Requires BlockUI/Structurize interaction in-game; the server-side work-order contract is covered separately above. |
| Structurize pack discovery and blueprint lookup | `AUTOMATED_PASS` | Structurize server-start loading discovers the nested MineColonies style packs; the Colonial Town Hall blueprint is resolved during the GameTest batch, including the builder fixture's explicit `fundamentals/townhall1.blueprint` assignment. |
| Serialization and saved colony data | `AUTOMATED_PASS` / `MANUAL_VALIDATION_PENDING` | Colony and citizen permission/NBT round-trips pass in the GameTest fixture; the complete custom-entity fixture also exercises reload behavior, with the two upstream non-persistent projectiles discarding by design; a real restart restored populated colony records and representative citizen/visitor entities. Full gameplay-cycle coverage and old-entity DataFixer migration remain pending/not provided by upstream. |
| Automated tests | `AUTOMATED_PASS` | The latest stable Fabric GameTest run in `logs/minecolonies-gametest-c2s-open-inventory-retry.log` reports `All 67 required tests passed`, including priority/inheritance, projectile-impact and fishing-event cancellation, the client-to-server Town Hall rename, colony-foundation, hut/building rename, colony style and management settings, Builder delivery priority, Farmer field assignment/settings, building reactivation, Miner level selection and repair work-order creation, Guard Tower patrol-mine assignment/clearing, building dirty marking, colony move-in policy, citizen pause/resume, Farmer hire/fire and hiring-mode transitions, creative citizen skill adjustment, Courier and Quarry hiring-mode transitions, work-order priority/removal, residence assignment/unassignment, free-interaction block/position permission changes, Builder inventory transfer, minimum-stock module add/remove, Guard Tower entity-filter add/remove, Smeltery item-filter add/remove/reset, GiveTool inventory binding, citizen inventory transfer, single-citizen recall, assigned-citizen worker/hut recall, Stone Smeltery recipe add/reorder/toggle and crafting-menu opening, colony color/help and spy-hiring control routes, citizen-inventory menu opening, deconstructed-building style, FarmField configuration, direct Town Hall placement, Builder work-order creation/selection, decoration work-order creation and University research envelopes, populated-tavern conversion and the 25-entity NBT fixture, followed by clean world and dimension saves. |

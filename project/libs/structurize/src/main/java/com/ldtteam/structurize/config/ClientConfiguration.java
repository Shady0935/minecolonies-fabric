package com.ldtteam.structurize.config;

/** Client-only configuration surface retained for API compatibility. */
public class ClientConfiguration extends AbstractConfiguration
{
    protected ClientConfiguration()
    {
    }
}
